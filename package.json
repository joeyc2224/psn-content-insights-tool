{
  "name": "psn-content-performance-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix api && npm install --prefix frontend",
    "dev": "concurrently \"npm --prefix api run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently \"npm --prefix api run start\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

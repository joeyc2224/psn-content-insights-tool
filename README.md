# psn-content-insights-tool
Joe Crosby - PSN technical exercise

## Installation
1. Install Node.js (LTS) which includes npm.
2. From the repo root:
```
npm install
npm run dev
```
3. Open the app at the Vite URL (usually `http://localhost:5173`) and the API at `http://localhost:3000/api/health`.

info:
  name: Video Detail
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/videos/kjBxIfn64KM"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
